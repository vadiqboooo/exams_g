# Публичная страница результатов для студентов

## Описание

Каждый студент получает уникальную ссылку для просмотра своих результатов экзаменов без необходимости авторизации.

## Как это работает

### 1. Генерация токенов

- При создании нового студента автоматически генерируется уникальный токен доступа
- Для существующих студентов токены были сгенерированы скриптом `backend/generate_tokens.py`

### 2. Получение ссылки на результаты (для администратора)

1. Войдите в систему как администратор
2. Перейдите на вкладку "Студенты"
3. Найдите нужного студента
4. Нажмите кнопку **"🔗 Ссылка на результаты"**
5. Ссылка автоматически скопируется в буфер обмена
6. Отправьте эту ссылку студенту любым удобным способом (Telegram, Email, SMS и т.д.)

### 3. Просмотр результатов (для студента)

1. Студент открывает полученную ссылку
2. На странице отображаются:
   - ФИО студента
   - Список всех его экзаменов
   - Для каждого экзамена:
     - Название экзамена
     - Предмет
     - Первичный балл
     - Тестовый балл (если применимо)
     - Ответы
     - Комментарий преподавателя (если есть)

### 4. Регенерация токена (если ссылка была скомпрометирована)

1. Войдите как администратор
2. Откройте форму редактирования студента
3. Поставьте галочку **"Регенерировать токен доступа к результатам"**
4. Сохраните изменения
5. Получите новую ссылку и отправьте студенту

**Важно:** После регенерации токена старая ссылка перестает работать!

## Безопасность

- Каждый токен уникален и имеет длину 43 символа (256 бит энтропии)
- Токены не содержат личной информации студента
- Публичная страница показывает только результаты экзаменов, без телефонов и других личных данных
- Без токена доступ к результатам невозможен

## Формат ссылки

```
https://your-domain.com/results/TOKEN
```

Пример:
```
https://example.com/results/o2Q5dQ4PqQcuFIs2xptfl66q-rQgIFb2GjR0FdB_BaQ
```

## Запуск приложения

### Backend
```bash
cd backend
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

### Frontend
```bash
cd frontend
npm run dev
```

## API Endpoints

### Публичный эндпоинт (без авторизации)
```
GET /public/results/{access_token}
```

Возвращает:
```json
{
  "fio": "Иванов Иван Иванович",
  "exams": [
    {
      "id": 1,
      "name": "Пробник 1",
      "subject": "rus",
      "answer": "1,2,3,4,5...",
      "comment": "Хорошая работа!",
      "exam_type_id": 1,
      "id_student": 1
    }
  ]
}
```

## Миграции

Миграция для добавления поля `access_token` уже применена:
```bash
cd backend
python -m alembic upgrade head
```

## Генерация токенов для существующих студентов

Если в будущем потребуется сгенерировать токены для студентов:
```bash
cd backend
python generate_tokens.py
```

## Технические детали

- **Backend**: FastAPI, SQLAlchemy, SQLite
- **Frontend**: React, React Router, Axios
- **Токены**: генерируются с помощью `secrets.token_urlsafe(32)`
- **Маршрутизация**: React Router для обработки публичных страниц
